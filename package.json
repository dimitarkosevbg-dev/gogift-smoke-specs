{
  "name": "gogift-smoke.spec.ts",
  "version": "1.1.0",
  "description": "End-to-end QA automation for shop.gogift.com — Playwright + TypeScript with visual regression and performance benchmarks.",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:regression": "playwright test --grep @regression",
    "test:visual": "playwright test --project=visual",
    "test:visual:update": "playwright test --project=visual --update-snapshots",
    "test:performance": "playwright test --project=performance",
    "test:lighthouse": "playwright test --project=performance --grep @lighthouse",
    "test:perf:no-lighthouse": "playwright test --project=performance --grep-invert @lighthouse",
    "report": "playwright show-report"
  },
  "keywords": [
    "playwright",
    "qa",
    "automation",
    "visual-regression",
    "performance"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "lighthouse": "^12.2.0",
    "playwright-lighthouse": "^4.0.0",
    "typescript": "^6.0.3"
  }
}
